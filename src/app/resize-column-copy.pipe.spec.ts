import { ResizeColumnCopyPipe } from './resize-column-copy.pipe';

describe('ResizeColumnCopyPipe', () => {
  it('create an instance', () => {
    const pipe = new ResizeColumnCopyPipe();
    expect(pipe).toBeTruthy();
  });
});
